<!doctype html>
<html lang="en">

<head>
	<meta charset="utf-8" />
	<title>removeAll</title>
</head>

<body>
    <h1>Removing All Movies</h1>

    <form action="/removeAllMovies" method="post">
        <input type="submit" id="remove" value="Remove all Favorites" onclick="confirmSubmission()">
    </form>
    <hr>
    <br>
    <a href="/">RETURN TO HOME PAGE</a>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            color: #333333;
            line-height: 1.6;
            padding: 20px;
        }

        h1 {
            color: #4787e7;
        }

        h2 {
            color: #0f6bd3;
        }

        a {
            color: #0f6bce;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }

        hr {
            margin-top: 20px;
            margin-bottom: 20px;
            border: 0;
            border-top: 1px solid #eee;
        }
    </style>
</body>

<script>
    function confirmSubmission() {
        // Get the port number from the URL
        var portNumber = window.location.port;

        // Create the confirmation message
        var confirmationMessage = "Are you sure you want to remove all movies from your favorites?\n";

        // Display the confirmation dialog box
        var confirmed = confirm(confirmationMessage);

        // If the user confirms, submit the form
        if (confirmed) {
            document.getElementById("applicationForm").submit();
        } else {
            // If the user cancels, do nothing
        }
    }
</script>
</html>